package controller;

import java.awt.event.KeyEvent;
import java.awt.event.KeyListener;

public class newMapSpaceBarr implements KeyListener {

    @Override
    public void keyTyped(KeyEvent e) {

    }

    @Override
    public void keyPressed(KeyEvent e) {

    }
    /*
        @Override
        public void keyPressed(KeyEvent e) {
            // Verifica se il tasto premuto è lo spazio
            if (e.getKeyCode() == KeyEvent.VK_SPACE) {
                // Esegui il codice desiderato quando viene premuto lo spazio
                System.out.println("Spazio premuto! Eseguiamo del codice...");
                // Inserisci qui il codice da eseguire
                // Ad esempio, potresti chiamare un metodo o avviare un'azione
            }
        }
    */
    @Override
    public void keyReleased(KeyEvent e) {

    }
}
